{
  "name": "@guardtab/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Types and Zod schemas shared between the GuardTab backend and extension",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "rimraf": "^6.0.1"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
